# SpectralBridge notebook vignettes

Open the notebooks in numeric order or choose the stage you need. The catalog,
purpose of each notebook, and setup instructions are in
[`../notebook-vignettes.md`](../notebook-vignettes.md).

All processing cells default to `RUN = False`. Edit the configuration cell,
confirm storage and memory, then set `RUN = True`.
